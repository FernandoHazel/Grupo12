<!DOCTYPE html>
<html lang="en">

<!-- Vista parcial del head -->
<%- include('./partials/head.ejs') %>
<body>
   <!-- Vista parcial del header -->
    <%- include('./partials/header.ejs') %>
    <br>
    <br>

    <!-- Contenido principal -->
    <main class="flexCarrito">
        
        <section class="tituloCarrito">
            <p>TU CARRO DE LA COMPRA</p>
            
        </section>

        <section class="productoDeCarrito"></section>
        <% for (let i=0 ; i < array.length ; i++ ) { %>
            <article>
                <div class="carritoImagen">
                    <img src="<%= array[i].img %>" id="logo">
                </div>
        
                    <div class="carritoDetalles">
                        <p class="nombreProducto"> <%= array[i].title %> </p>
                        <P class="detallesProducto"> <%= array[i].description %> </P>
                        <p class="cantidadProducto">CANTIDAD</p>
                    </div>
        
                    <div class="carritoPrecio">
                        <p class="precioProducto"> $<%= array[i].price %> MXN</p>
                        <span class="eliminarProducto"><a href="/users/borrarProductoCarrito/<%= array[i].id %>">Eliminar</a></span> 
                    </div>
            </article>
        <% }  %>
    </section>

        <section class="comprarCarrito">
            <div>
                <form action="">
                    <label for="opciones">Método de pago</label>
                    <br>
                    <select name="opciones" >
                
                        <option value="1">Opción 1</option>
                        <option value="2">Opción 2</option>
                        <option value="3">Opción 3</option>
                    </select>
                </form>
            </div>
            <div>
                TOTAL: $9,460 MXN
            </div>

            <div >
                <button class="eliminar">ELIMINAR TODO EL CARRITO</button>
            </div>

            <div>
                <button class="comprar">COMPRAR</button>
            </div>
        </section>

    </main>
    <br>

     <!-- Vista parcial del footer -->
     <%- include('./partials/footer.ejs') %>
</body>
</html>